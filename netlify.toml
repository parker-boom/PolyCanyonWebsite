[build]
  command = "npm run check"
  publish = "build"

[build.environment]
  NODE_VERSION = "22"

# Preserve the archive's established mixed-case slugs.
[build.processing.html]
  pretty_urls = false
